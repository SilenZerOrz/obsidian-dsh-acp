# Dependencies
node_modules/

# Runtime state
logs/
*.log

# Temp / cache
tmp-cache/
.tmp/

# OS
.DS_Store

# Editor
*.swp

# Publish artifacts
*.tgz

# Installer backups
.install-backups/

# Alternate package-manager lockfiles (project canonical = package-lock.json)
pnpm-lock.yaml

# Local-only dev docs (requirements / planning). Kept off git until we decide
# to commit them ("后续补上传 git"). Tracked only on the developer's machine.
docs/

# npm pack test artifacts (do NOT commit)
package/
obsidian-dsh-acp-*.tgz

# awesome-dsh-plugin submission draft (do NOT commit — local-only)
awesome-dsh-plugin/

# Local dev test fixtures (need the global dsh install; not portable)
test/fixtures/

# Local-only test suite + dev scripts (not part of the npm package)
test/
scripts/
acp-feature-test.mjs
test-validate-cwd.mjs
